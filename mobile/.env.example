# Copy to .env and set your machine's LAN IP when testing on a real phone (Expo Go).
# Find IP: Windows `ipconfig`, Mac/Linux `ipconfig getifaddr en0` or check Wi‑Fi settings.
# Backend must run (npm run dev in /backend) and listen on 0.0.0.0:8000.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
